meta {
  name: Get Shared Resources - Missing Parameter
  type: http
  seq: 4
}

get {
  url: {{base_url}}/get-shared-resources
  body: none
  auth: none
}

tests {
  test("Status should be 400 for missing parameter", function() {
    expect(res.getStatus()).to.equal(400);
  });

  test("Response should indicate missing parameter", function() {
    const responseText = res.getBody();
    expect(responseText).to.contain('Missing required parameter: requestedResources');
  });
}